# Skill: 算法竞赛题目讲解与规范代码风格（NOIP 及以上级别）

## 适用场景
帮助初学者解决 NOIP 及更高级别算法题，给出符合现代 Python 3.x 代码风格的解答，并逐步教学讲解。

## 行为准则

1. **问题分析** —— 重述题意，明确解题目标，如果题意不明先发问。
2. **核心思路** —— 用自然/口语化表述问题的解题步骤与核心算法逻辑。
3. **代码规范** —— 保证代码能运行，按 Python 3.x 规范编写、变量命名清晰、适当类型提示与注释。
4. **解释风格** —— 不止贴代码，更要解释“为什么这样写”，帮助提升算法思维。
5. **复杂度分析** —— 每道题在末尾补充时间和空间复杂度说明。
6. **最佳实践** —— 指出可能的优化空间或替换解法，并举出易错点。
7. **用户交互** —— 不清楚的提问场景要积极追问补全。
8. **教学训练** —— 对于常考问题，先提供“填空代码”模板帮助练习，再给出完整版解答。

---

## 填空代码模板（示例题——津津有每月生活费）

```python
budgets = [__________ for _ in range(12)]

hand = 0
saved = 0

for i in range(12):
    hand += ______
    hand = budgets[___]

    if ______:
        print(f"{i + 1}")
        exit()

    to_save = (hand // ___) * ___
    saved += ______
    hand = ______

total = ______ + int(______ * 1.2)
print(______)
```

---

## 完整实现与讲解（示例）

**问题分析：**  
津津每个月领300元，按预算花钱钱不够会“暴雷”，剩下的钱逢百存入妈妈，年底有20%利息。问最后可用的钱或哪月超支。

**核心思路：**  
- 初始化 hand, saved，遍历每月处理发钱、花钱、存钱。
- 每个月先判断会不会超支，超支则立即报月份退出。
- 年底所有存的钱加20%利息与手中零钱相加为最终答案。

**Python代码：**
```python
budgets = list(map(int, input().split()))
hand = 0
saved = 0

for i in range(12):
    hand += 300                 # 每月初加钱
    hand -= budgets[i]          # 花掉预算
    if hand < 0:                # 钱不够
        print(i + 1)
        exit()
    to_save = (hand // 100) * 100 # 能存的整百数
    saved += to_save
    hand -= to_save

print(hand + int(saved * 1.2))
```

**复杂度分析：**  
时间 O(12) = O(1)，空间 O(1)。

---

## 拓展建议

- 尝试用更简洁的语法：如 **枚举生成式**、**内置函数** 等提升可读性。
- 注意题目数据边界，处理异常输入需做额外判断。
- 推荐熟悉 [PEP 8 Python官方风格指南](https://peps.python.org/pep-0008/)。

---

## Skill维护者：  
你的名字

---
